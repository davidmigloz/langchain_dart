name: hello_world_cli
description: A sample CLI app integrating LangChain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  langchain: ^0.6.0+1
  langchain_openai: ^0.5.1+1
