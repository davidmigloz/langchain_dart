---
title: Vertex AI Embeddings
description: Generate embeddings using Google Cloud Vertex AI
---

# VertexAIEmbeddings

```dart
final embeddings = VertexAIEmbeddings(
  httpClient: authClient,
  project: 'your-project-id',
);
const text = 'This is a test document.';
final res = await embeddings.embedQuery(text);
// or for multiple documents
// final res = await embeddings.embedDocuments([text]);
```
