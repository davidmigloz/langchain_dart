## 0.0.1-dev.2

- Add `ChatCohere` (wrapper around the Cohere v2 Chat API with support for `invoke`, `stream` and tool calling) and `CohereEmbeddings` (wrapper around the Cohere v2 Embed API with `search_document`/`search_query` input type handling).

## 0.0.1-dev.1

- Bootstrap project.
